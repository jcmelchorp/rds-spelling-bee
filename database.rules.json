{
  "rules": {
    "users": {
      "$userId": {
        ".read": "auth.uid",
        ".write": "$userId === auth.uid"
      }
    },
    "wordlists": {
      "$id": {
        ".read": "auth.uid",
        ".write": "false"
      }
    }
  }
}